# Only the packages NOT already in dromni/nerfstudio:1.1.4.
# The base image provides torch, torchvision, numpy, scipy, opencv, Pillow, tqdm,
# rich, typer, tinycudann, gsplat, and nerfstudio itself — don't re-pin them here
# or you risk fighting the base image's locked versions.

anthropic==0.39.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
httpx==0.27.2
