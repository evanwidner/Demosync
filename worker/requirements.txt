# Packages the worker needs that aren't reliably importable from the system
# python in dromni/nerfstudio:1.1.4. We keep this minimal and let pip resolve
# any sub-deps — torch / torchvision / numpy / scipy / opencv / Pillow /
# tinycudann / gsplat / nerfstudio itself stay untouched (they're on the
# base image's Python path).

anthropic==0.39.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
httpx==0.27.2

# CLI + logging — imported at module load time by worker.pipeline.
typer==0.12.5
rich==13.8.1
