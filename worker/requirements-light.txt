# Subset of worker/requirements.txt that installs cleanly on macOS / laptop without
# CUDA + nerfstudio. Enough to run the Claude stages (organize, camera path, QA,
# voiceover) and the ffmpeg post stage. Use for local dev when full reconstruction
# happens on RunPod.

anthropic==0.39.0
boto3==1.35.36
python-dotenv==1.0.1
ffmpeg-python==0.2.0
typer==0.12.5
rich==13.8.1
numpy<2
Pillow==10.4.0
psycopg[binary]==3.2.3
